export default function HeroTable({
    columns,
    rows,
}: {
    columns: { header: string; accessorKey: string }[];
    rows: { [key: string]: string }[];
}) {
    return (
        <div>
            <h2>Teste</h2>
        </div>
    );
}
